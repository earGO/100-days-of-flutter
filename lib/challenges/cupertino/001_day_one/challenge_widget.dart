import 'package:flutter/cupertino.dart';

class DayOneChallengeWidget extends StatelessWidget {
  const DayOneChallengeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Day one challenge');
  }
}
